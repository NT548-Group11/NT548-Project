pipeline {
    agent {label "jenkins-agent"}

    stages {
        stage('Build') {
            steps {
                sh 'pwd; ls -la'
                ///
            }
        }
        // stage('Test') {
        //     steps {
        //         echo 'Testing..'
        //     }
        // }
        // stage('Deploy') {
        //     steps {
        //         echo 'Deploying....'
        //     }
        // }
    }
}